name: deployment-checks

# CI + DB migration + production deploy for every push to main. All jobs
# live in ONE workflow on purpose:
#
#   - Ordering: GitHub has no cross-workflow dependencies. Here
#     `secretlint` runs FIRST and gates the whole pipeline (a committed
#     secret must never reach prod), then `check` and `test` run in
#     PARALLEL, then `migrate-db` needs both, then `deploy` needs the
#     migration, then `pdf-ocr-smoke` needs the deploy. That is the same
#     order scripts/deploy uses, and it is why main is NOT deployed by
#     Vercel's own Git integration (`vercel.json` sets
#     `git.deploymentEnabled.main = false`): auto-deploy put the new code
#     live while the DDL was still pending, so a red test run shipped code
#     whose table did not exist yet (EXPENSE-18, 2026-09-20). PR previews
#     are untouched and still deploy from Git.
#   - DB migrations run ONLY after the test suite passes, never before.
#     Migrating prod for code whose tests haven't passed would leave the
#     schema ahead of the deployed bundle. So the contract is:
#         secretlint → check & test → migrate prod DB → deploy → smoke
#   - Time budget: there is no workflow-level timeout in GitHub Actions,
#     so the whole run is bounded by the job timeouts on the critical
#     path: secretlint 2m + max(check 2m, test 4m) + migrate 2m + deploy 5m
#     + smoke 15m = an ~28m ceiling (typical runs finish in ~9m; PR-only
#     runs are just secretlint + check + test, ~3m). These are fail-fast
#     guards, not targets: a hung job dies here instead of running for the
#     default 6h.
#   - Emails: GitHub sends one notification email per failed workflow run.
#     Two workflows meant up to two emails per push; one workflow means at
#     most one.
#
# The post-deploy PDF/OCR/MCP smoke check runs after every production
# deploy: from scripts/deploy (CLI deploys, with rollback on failure) and
# from the pdf-ocr-smoke job below (the push path, also with rollback).
# Both share scripts/smoke-check against /api/smoke.
#
# The migrate-db job passes the prod DDL URL directly via the
# DATABASE_URL_UNPOOLED GitHub secret (the same session-pooler URL
# scripts/deploy uses for schema DDL) and runs the same `prisma db
# update` sync as scripts/deploy step 3, through a local pgbouncer
# (the linux CLI build ships without TLS; scripts/deploy runs on
# darwin where the CLI connects directly). The CI VERCEL_TOKEN can't
# read project settings (403 PROJECT_UNAUTHORIZED) so `vercel env
# pull` doesn't work here; the deploy job below needs only project
# identity, which comes from .vercel/repo.json (committed values below).
#
# NOTE: the job names are stable; keep them that way.

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  # Whole-repo secret scan (tokens, keys, credentials); runs FIRST and
  # gates everything below: check/test/migrate don't start until it
  # passes, so a committed secret never reaches prod. Runs on PRs too.
  # --format github emits inline PR annotations on failures; .gitignore is
  # respected (secretlint v13+), so gitignored files (.env*, node_modules,
  # prisma/backup.sql) are never scanned. The scan set is exactly what
  # could reach the repo. Config: .secretlintrc.json
  secretlint:
    name: Secretlint
    runs-on: ubuntu-latest
    # The secretlint action self-installs from the npm registry inside the
    # job; a slow registry (2026-09-03/04 outage) stalled that install past
    # 3 minutes. 10m matches the test job's headroom — the scan itself is
    # still fast, this only bounds the install.
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v7

      - uses: pnpm/action-setup@v6

      - uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      - run: pnpm exec secretlint --format github "**/*"

  # Static checks (fmt, lint, types): no database, no browsers. Runs in
  # parallel with the test job (after the secretlint gate).
  check:
    name: Check
    runs-on: ubuntu-latest
    timeout-minutes: 5
    needs: [secretlint]
    steps:
      - uses: actions/checkout@v7

      - uses: pnpm/action-setup@v6
        # No `version` input: read pnpm@11.20.0 from package.json's
        # packageManager field so the runner can never drift from the repo.
        # Pinning a version here made the action fail with
        # ERR_PNPM_BAD_PM_VERSION when the two disagreed.

      - uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      - run: pnpm check

  # Full test suite against a throwaway Postgres. The Playwright browser
  # tests (closed-reports, editor-create-report, home, mileage) add ~2m
  # on top of the ~2m unit-test core; a cold-cache run with browser
  # install hits ~5-6m, so 10m leaves headroom for flaky CI runners.
  test:
    name: Test
    runs-on: ubuntu-latest
    timeout-minutes: 15
    needs: [secretlint]
    services:
      # Matches the hardcoded test URL in vite.config.ts:
      # postgres://assaf@localhost/expense_test (passwordless)
      postgres:
        image: postgres:18
        env:
          POSTGRES_USER: assaf
          POSTGRES_DB: expense_test
          POSTGRES_HOST_AUTH_METHOD: trust
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U assaf"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    env:
      # initStore bootstraps an account from these on an empty database
      # (tests force-reset the schema, so they must be present).
      APP_EMAIL: testuser@example.com
      APP_PASSWORD: test-password
      SESSION_SECRET: ci-only-session-secret-0123456789abcdef
      # Runs the real tesseract OCR round-trip in test/pdf-ocr.test.ts
      # (downloads eng.traineddata from a CDN; skipped locally by default).
      RUN_OCR_TESTS: "1"
      # Defense-in-depth: launchServer already pins this for the spawned
      # server, but CI has no .env, so keep the /emails page on the configured
      # branch even if the pin ever regresses (Aug 2026 flake, see
      # docs/../commit 014a9c6f).
      EMAIL_TOKEN_ENCRYPTION_KEY: AQEBAQEBAQEBAQEBAQEBAQEBAQEBAQEBAQEBAQEBAQE=
      # Same defense-in-depth for the Gmail connect buttons on /emails.
      GOOGLE_OAUTH_CLIENT_ID: test-gmail-client-id
      GOOGLE_OAUTH_CLIENT_SECRET: test-gmail-client-secret
      GOOGLE_PUBSUB_TOPIC: projects/test/topics/expense-test
      FASTMAIL_OAUTH_CLIENT_ID: test-fm-client-id
    steps:
      - uses: actions/checkout@v7

      - uses: pnpm/action-setup@v6

      - uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      # Emit the contract artifacts (contract.json/d.ts are committed, but
      # re-emitting keeps the build honest about drift on a fresh checkout.
      - run: pnpm build:prisma

      # Playwright browsers are not installed by pnpm install; the E2E tests
      # launch headless Chromium, so download it (with OS deps) first, and
      # cache the browsers so the ~20-40s install only happens once per lockfile.
      - name: Cache Playwright browsers
        uses: actions/cache@v5
        with:
          path: ~/.cache/ms-playwright
          key: playwright-${{ runner.os }}-${{ hashFiles('pnpm-lock.yaml') }}

      - run: pnpm exec playwright install --with-deps chromium

      - run: pnpm test

  # Production schema sync: runs ONLY after the test suite passes (never
  # before; a migration applied for code that later fails CI would leave prod
  # ahead of the deployed bundle).
  migrate-db:
    name: Migrate prod DB
    runs-on: ubuntu-latest
    # Must fit checkout + pnpm install + pgbouncer + prisma db update. The
    # budget has been raised twice as installs grew: 1m (the
    # pnpm/action-setup self-installer alone took 27s on 2026-08-06), then
    # 3m, then 10m after the 2026-09-03/04 npm-registry slowness stalled
    # installs past 3 minutes and cancelled the job before db push.
    timeout-minutes: 10
    needs: [check, test]
    # `success()` keeps the default skip-on-failure semantics (no migration
    # when check or test failed) on top of the main-only gate.
    if: success() && github.ref == 'refs/heads/main'
    env:
      # Prod DDL URL (Supabase session pooler), the same value scripts/deploy
      # pulls from Vercel as DATABASE_URL_UNPOOLED. Required; the job fails
      # fast if the secret is missing.
      DATABASE_URL_UNPOOLED: ${{ secrets.DATABASE_URL_UNPOOLED }}
    steps:
      - uses: actions/checkout@v7

      - uses: pnpm/action-setup@v6

      - uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      # Auto-deploys (git push to main) reach prod without scripts/deploy,
      # so this job is the schema-sync path for them: same command and URL
      # as scripts/deploy step 3. The psql precheck fails fast with a
      # sharper error than prisma's on network/secret issues.
      - name: Verify prod database connectivity
        run: |
          psql "$DATABASE_URL" -c 'SELECT 1' -tA
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL_UNPOOLED }}

      # The linux Prisma CLI build ships without TLS, and Supabase rejects
      # plaintext with (ESSLREQUIRED): every sslmode in the URL is ignored
      # (probed on 2026-08-31; the darwin CLI does TLS, which is why
      # scripts/deploy connects directly). A local pgbouncer bridges it:
      # prisma speaks plain postgres to it on loopback, pgbouncer speaks
      # TLS to the pooler. auth_type=trust + auth_file is required, not
      # auth_type=any: with any, pgbouncer sends an empty SCRAM username
      # and the server rejects it. The pooler host is passed as an IP
      # because pgbouncer's own resolver is unreliable.
      - name: Sync prod schema
        run: |
          HOST=$(echo "$DATABASE_URL_UNPOOLED" | sed -E 's|^[^@]+@||; s|:[0-9]+.*$||')
          PORT=$(echo "$DATABASE_URL_UNPOOLED" | sed -E 's|^[^@]+@[^:]+:([0-9]+).*|\1|')
          PGUSER=$(echo "$DATABASE_URL_UNPOOLED" | sed -E 's|^postgres(ql)?://([^:]+):.*|\2|')
          PGPASS=$(echo "$DATABASE_URL_UNPOOLED" | sed -E 's|^postgres(ql)?://[^:]+:([^@]+)@.*|\2|')
          IP=$(getent hosts "$HOST" | awk 'NR==1{print $1}')
          echo "pooler: $HOST ($IP:$PORT)"
          mkdir -p /tmp/pgb
          printf '[databases]\npostgres = host=%s port=%s dbname=postgres\n[pgbouncer]\nlisten_addr = 127.0.0.1\nlisten_port = 6433\nauth_type = trust\nauth_file = /tmp/pgb/userlist.txt\nserver_tls_sslmode = require\npool_mode = session\nignore_startup_parameters = extra_float_digits,options\npidfile = /tmp/pgb/pgbouncer.pid\nlogfile = /tmp/pgb/pgbouncer.log\n' "$IP" "$PORT" > /tmp/pgb/pgbouncer.ini
          printf '"%s" "%s"\n' "$PGUSER" "$PGPASS" > /tmp/pgb/userlist.txt
          sudo apt-get install -y pgbouncer >/dev/null
          # The package auto-starts a system pgbouncer on the default 6432
          # with an empty userlist; stop it so ours owns its own port.
          sudo systemctl stop pgbouncer 2>/dev/null || true
          sudo systemctl disable pgbouncer 2>/dev/null || true
          pgbouncer -d /tmp/pgb/pgbouncer.ini
          DB_NAME=$(printf '%s' "$DATABASE_URL_UNPOOLED" | sed -E 's|.*/([^/?]+)(\?.*)?$|\1|')
          export DATABASE_URL="postgres://$PGUSER:$PGPASS@127.0.0.1:6433/$DB_NAME?sslmode=disable"
          # The CLI prefers DATABASE_URL_UNPOOLED over DATABASE_URL when
          # both are set; point both at the loopback pgbouncer.
          export DATABASE_URL_UNPOOLED="$DATABASE_URL"
          for i in 1 2 3 4 5; do
            psql "$DATABASE_URL" -c 'SELECT 1' -tA && break
            if [ "$i" = 5 ]; then
              echo "pgbouncer never came up; its log:" >&2
              tail -n 20 /tmp/pgb/pgbouncer.log >&2 || true
              exit 1
            fi
            sleep 1
          done
          pnpm prisma db update --no-interactive --confirm "$DB_NAME"

  # Production deploy, AFTER the schema sync: the same order scripts/deploy
  # uses (its steps 1-3 are the check, test and migrate-db jobs above).
  # Vercel's own Git integration is off for main (vercel.json), because it
  # put the new code live while the DDL was still pending: a red test run
  # shipped code whose table did not exist yet (EXPENSE-18, 2026-09-20).
  # With auto-deploy off, a missing token is a failure, not a skip: nothing
  # else would reach prod.
  deploy:
    name: Deploy prod
    runs-on: ubuntu-latest
    # Production builds take ~2m; 5m bounds a hung CLI.
    timeout-minutes: 5
    needs: [migrate-db]
    if: success() && github.ref == 'refs/heads/main'
    outputs:
      url: ${{ steps.deploy.outputs.url }}
    env:
      VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
      # Project identity for a non-interactive link. These are the
      # repo→project values in .vercel/repo.json, not credentials — and they
      # are what puts the deploy on the right team: passing `--scope` here
      # instead makes the CLI resolve the token's user first (getUser), which
      # this token cannot do (404 "User not found", run 35544142703), because
      # it has no personal identity.
      VERCEL_ORG_ID: team_bjyg9pgn8TQQVP2NLMPnSYSN
      VERCEL_PROJECT_ID: prj_7VcQaMHQ0xtjXnNktAlSmqlVpAai
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          node-version: 24

      # The same CLI call scripts/deploy makes, with the same URL scrape
      # from its output. The smoke job below takes the URL from this job's
      # output, so no step here has to match deployments by commit SHA.
      - name: Deploy to production
        id: deploy
        run: |
          set -o pipefail
          if [ -z "$VERCEL_TOKEN" ]; then
            echo "VERCEL_TOKEN is not configured — main no longer deploys from Git, so there is nothing else to ship this commit." >&2
            exit 1
          fi
          npx vercel@latest deploy --prod --yes --token "$VERCEL_TOKEN" | tee /tmp/deploy.log
          URL=$(grep -oE 'https://[a-zA-Z0-9.-]+\.vercel\.app' /tmp/deploy.log | tail -1 || true)
          if [ -z "$URL" ]; then
            echo "Could not read the deployment URL out of the CLI output." >&2
            exit 1
          fi
          echo "Deployed: $URL"
          echo "url=$URL" >> "$GITHUB_OUTPUT"

  # Post-deploy PDF/OCR/MCP smoke + auto-rollback for the deploy job above.
  # Runs the same smoke scripts/deploy uses against the deployment that job
  # just made, and rolls it back if it fails. Skips (with a missing-secret
  # message in the logs) until the SMOKE_TEST_SECRET and
  # VERCEL_PROTECTION_BYPASS GitHub secrets exist; CLI deploys still gate via
  # scripts/deploy.
  pdf-ocr-smoke:
    name: Smoke prod + rollback
    runs-on: ubuntu-latest
    timeout-minutes: 15
    needs: [deploy]
    # No secrets in this condition: the secrets context is rejected in
    # job-level `if` (workflow file error). Missing secrets are handled
    # inside the step below, which skips the gate with a notice.
    if: success() && github.ref == 'refs/heads/main'
    env:
      VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
      SMOKE_TEST_SECRET: ${{ secrets.SMOKE_TEST_SECRET }}
      VERCEL_PROTECTION_BYPASS: ${{ secrets.VERCEL_PROTECTION_BYPASS }}
      # The rollback below is a CLI call, so it needs the same project
      # identity and the same "no --scope" rule as the deploy job: `--scope`
      # makes the CLI resolve the token's user, which 404s for this token.
      VERCEL_ORG_ID: team_bjyg9pgn8TQQVP2NLMPnSYSN
      VERCEL_PROJECT_ID: prj_7VcQaMHQ0xtjXnNktAlSmqlVpAai
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          node-version: 24

      # Smoke the deployment the deploy job just made, and roll it back on
      # failure. One step on purpose: the smoke/rollback flow must not
      # depend on cross-step state. Skipped with a notice when the smoke
      # secrets aren't configured (CLI deploys still gate via
      # scripts/deploy).
      - name: Smoke production deployment + rollback on failure
        run: |
          if [ -z "$SMOKE_TEST_SECRET" ] || [ -z "$VERCEL_PROTECTION_BYPASS" ] || [ -z "$VERCEL_TOKEN" ]; then
            echo "Smoke secrets not configured as GitHub secrets — skipping the smoke gate."
            echo "Add SMOKE_TEST_SECRET and VERCEL_PROTECTION_BYPASS to enable it."
            exit 0
          fi
          URL="${{ needs.deploy.outputs.url }}"
          if [ -z "$URL" ]; then
            echo "The deploy job reported no URL — nothing to smoke." >&2
            exit 1
          fi
          echo "Deployment: $URL"
          if ./scripts/smoke-check "$URL"; then
            echo "Smoke passed — deployment stays live."
          else
            echo "Smoke FAILED on $URL — rolling back to the previous deployment." >&2
            npx vercel@latest rollback "$URL" --yes || echo "Rollback failed — check the Vercel dashboard." >&2
            exit 1
          fi
